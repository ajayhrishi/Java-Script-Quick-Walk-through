import * as op from "../../OutPut.mjs"
op.Create_MH("Mainhead",`This page is to test spread operator`);
op.Create_sH("p1",`Using this page, I will be testing some basic usage of the spread operator in arrays`);


op.Create_R("In", `hellow there please enter the values in the below box, so that we can test the spread operator using it
<br>If you want to use the deault array that's already in place, then click button "Default Array"`);


